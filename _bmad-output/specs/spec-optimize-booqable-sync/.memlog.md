---
topic: Optimize shared Booqable sync
updated: 2026-09-24T11:14
---

- (direction) Source: conversation. Produce a feature spec only; no application implementation, story breakdown, migrations or deployment authorized.
- (assumption) Use slug optimize-booqable-sync derived from the task and existing branch; two confirmed capabilities. Batching scope remains a user question. Missing project-context.md supplied no extra rules; AGENTS.md applies.
- (capability) CAP-1: Refresh orders relevant to the requested period without scanning all reserved orders, while rechecking locally eligible orders so cancellations or moved dates clear stale local data. Source: user accepted date filtering and stale-candidate annotation.
- (capability) CAP-2: Keep accurate listed/succeeded/failed/skipped totals without recounting every accumulated result after each order. Retry replacement and duplicate processing must preserve exact totals. Source: user accepted recount optimization.
- (constraint) Reuse existing shared reconciliation and run infrastructure for Workshop and future Dashboard callers; no separate Dashboard engine. Preserve authorization, preview/staging gate, order leases/fences, task ownership, assignment history, fulfillment guards, per-order transaction and source notices.
- (constraint) Workshop next-seven-days remains reserved scheduled starts in Madrid; all-reserved remains unbounded by dates. Dashboard target is reserved/started/stopped with scheduled start OR return in selected Madrid period, including next complete month. Dashboard UI remains separate. Source: delegated task and data-contract context.
- (constraint) Freeze dates/status/query semantics for a run and its retries; union upstream discoveries with locally eligible IDs selected in PostgreSQL; deduplicate before work without suppressing failed/uncertain retries. Count candidates actually examined, not excluded remote population.
- (decision) Accounting design stays open between batched upsert/recount and transactionally correct old/new deltas. All aggregation stays in PostgreSQL. Preserve safe replay if apply succeeds before result recording; never mark incomplete work successful.
- (note) Evidence sources: research/booqable-sync-optimization/research.md and experiment/findings.md, plus sanitized result JSON and scripts. Initial code baseline fb5dea7. Twenty authorized read-only HTTP requests: GET list omits deep graph, POST search simple IDs returns matching related resources for three reserved orders, two exact graphs and one unexplained primary-order difference; only one allocation sampled.
- (constraint) No batching implementation merely because parser succeeds: missing relationships may appear as removed allocations. Verify independent completeness, field differences, parser parity, many allocations, started/stopped/mixed states, pagination and lease-before-fetch before adopting bulk reads.
- (note) Research options retained as deferred: bounded concurrency, durable per-order continuation/background workers, cross-instance request budgets and change-marker incremental refresh. These were recommendations, not user-approved requirements. Detailed constraints retained in verification companion if revisited.
- (constraint) Measure request/RPC/recount work on deterministic fixtures; no promised percentage or runtime. Verify deployment and DB execution limits before rollout; local simulation is not tenant performance. No credentials/customer payloads in artifacts.
- (question) Q1: Is batching a follow-up experiment or an optional feature capability gated on verification? Asked asynchronously; await response before treating it as accepted.
- (question) Q2: Is arbitrary selected-period support delivered in this change or only its shared contract prepared for the Dashboard task? Current draft assumes shared support belongs here, Dashboard UI does not; confirmation needed before implementation.
- (event) Wrapper-only content: research process logs, editorial review narration, freshness-helper failure and duplicated prose omitted from contract; technical findings and unverified options preserved. Original research backed up to /private/tmp/booqable-research-backup-20260922-171340; JSON compacted with equality checks.
- (event) Coherence pass: two stable intent/success capabilities; explicit constraints/non-goals/testable work-count signal. Q1 batching and Q2 delivery boundary remain open; no unapproved optimization promoted into scope.
- (event) Preservation pass: accepted directions, Dashboard context, safety boundaries, retry semantics, experiment findings/limits and deferred options retained in kernel and two companions. Wrapper-only omissions explicitly recorded. No stories generated.
- (decision) Q1 resolved by user: include batching if verification passes. This supersedes earlier unapproved/follow-up status; failure of the gate does not block CAP-1 or CAP-2.
- (capability) CAP-3: Refresh multiple orders from one detailed provider response when equivalence/completeness and safe freshness are verified. Success requires the verification companion gate, equivalent normalized per-order outcomes, fewer detail requests for the same sample, explicit incomplete-result rejection and individual-fetch fallback if verification fails.
- (event) Updated coherence/preservation passes: CAP-1/2 preserved; stable CAP-3 added from user answer, Q1 resolved, Q2 remains open. Conditional ship/fallback behavior captured without inventing a batch size or runtime target.
- (decision) Q2 resolved by user: include selected-period support in this feature, including Next Month and reserved/started/stopped orders whose scheduled starts OR returns fall in the Madrid period. Dashboard task owns UI and caller integration. Supersedes the scope assumption; no implementation authorized this turn.
- (capability) CAP-1 clarified without changing ID: shared sync supports selected periods as well as existing Workshop scopes; success includes starts-only, returns-only, both dates, next month and stale-local-candidate reconciliation.
- (event) Coherence pass: all three stable intent/success capabilities retained; selected-period support is explicit, Q2 closed, batching remains gated, and Dashboard UI/integration excluded. Remaining listed-count assumption retained; no open product-scope questions.
- (event) Preservation pass: user scope resolution propagated to kernel, sync contract and verification. Prior retry, date, safety and experiment evidence constraints preserved. No stories or application changes.
- (direction) 2026-09-24 user update: revise the existing optimize-booqable-sync spec against current staging; recognize delivered Dashboard work, focus remaining date filtering on Workshop and consolidate shared run management, retain CAP-2 and conditional CAP-3; documentation only.
- (decision) User correction supersedes earlier all-reserved product scope: Workshop exposes only Sync next 7 days; All Reserved was removed because requests took too long. Existing all_reserved backend types/RPC/history are legacy compatibility, not a feature to restore, extend or optimize in this change.
- (note) Current code baseline 7a6f9d3883b490b543f364fd1e364b96a6e72471 (application baseline efeee06), inspected 2026-09-24: Dashboard already filters upstream starts and returns separately, captures local candidates in PostgreSQL, deduplicates IDs, saves scope/progress, and reconciles up to ten orders per action. Both callers share reconcileBooqableOrder and fetchSourceOrderDocument; each detail read remains individual and sequential. Dashboard delegates result recording to the existing full-run recount function.
- (capability) CAP-1 refined with stable ID: Workshop Next 7 Days and Dashboard selected dates use shared discovery/run/recovery infrastructure without redundant engines; Workshop adds upstream reserved-start date filtering and local stale-candidate rechecks, while Dashboard retains implemented reserved/started/stopped start-OR-return behavior including Next Month.
- (constraint) Generalize the existing Dashboard saved-candidate and bounded continuation path for both callers; preserve frozen Madrid dates, server-validated scope/policy, deduplication, per-order reconciliation, history/freshness attribution and interrupted-run recovery. Adapt existing page actions/continuation only as needed; no new sync button or UI redesign. Do not treat Workshop seven-day completion as Dashboard coverage or reinterpret legacy saved cursors as the new policy.
- (decision) Existing client-driven bounded continuation is now the reuse baseline and its extension to Workshop is accepted, superseding its earlier deferred-only status. Preserve pending/completed/failed evidence and explicit interruption; navigation can stop future chunks. Background autonomous execution, concurrency tuning and timestamp skipping remain deferred.
- (constraint) CAP-2 applies to both callers through their shared result accounting, with PostgreSQL aggregation, exact retry replacement/duplicate/skip semantics and safe apply-before-result replay. Choice between checkpoint recounts and transactionally correct result deltas remains implementation design.
- (constraint) CAP-3 remains conditional on the existing complete-graph/parity/pagination/freshness/fewer-detail-requests gate. Dashboard batches of ten are server-action work chunks, not bulk provider responses, and provide no new evidence for CAP-3; retain individual-read fallback and ship CAP-1/CAP-2 if gate fails.
- (note) Evidence scope: current source and SQL inspected, no live tenant call or benchmark. Dashboard Story 1.4 records local tests but explicitly leaves actual provider pagination, reconciliation throughput and deployment unverified. Historical research remains evidence at its original baseline; brownfield companion will distinguish implemented behavior from remaining requirements. project-context.md is absent; root AGENTS.md applies.
- (assumption) Retain listed as unique candidates examined; upstream-excluded remote rows are not skipped results. Existing all_reserved backend compatibility is retained unless separately scoped; user removal of its button does not authorize deleting stored history or legacy RPC contracts.
- (event) Update activation complete: customization/config resolved with temporary uv cache after sandbox cache denial; no prepend/append/on_complete hooks configured. Rich input distilled directly in place, slug and CAP-1/CAP-2/CAP-3 preserved; no story breakdown requested.
- (event) Coherence pass passed: CAP-1/CAP-2/CAP-3 remain stable with intent/success pairs; current Dashboard reuse, Workshop-only seven-day UI, shared accounting and conditional bulk detail fetch are consistent across kernel and three companions. Assumptions remain listed-count semantics and legacy backend compatibility; no unresolved product-scope question. Accounting mechanism and CAP-3 evidence remain explicit implementation choices/gates.
- (event) Preservation pass passed: prior date/status/DST, stale-candidate, retry/replay, PostgreSQL, authorization/RLS, lease/freshness, task/history/lifecycle, pagination, experiment limits, measurement and rollout constraints retained. User direction supersedes future-Dashboard wording, active all-reserved scope and deferred-only continuation. Wrapper-only content: repeated conversation explanations and tool/cache narration omitted; historical research documents retain their original baseline, clarified by brownfield.md.
- (event) Spec update finalized: re-derived SPEC.md, brownfield.md, sync-contract.md and verification.md from canonical decisions. Document checks passed for required kernel fields, unique stable IDs, intent/success pairs and companion/source paths; git diff --check passed. No stories, application code, SQL, provider calls or runtime tests produced.
- (direction) User explicitly adds all_reserved retirement to this spec only: prohibit starting/executing it at server-action and database entry points, preserve historical runs, retire unfinished runs without reinterpretation, retain only next_7_days and selected_period page-level scopes through shared functionality, and preserve individual task refresh. Supersedes the earlier assumption that executable legacy compatibility must remain.
- (note) Retirement inspection: MANUAL_SYNC_SCOPES and v1 cursor decoder still accept all_reserved; public/private workshop start/resume RPCs allow it and resume any non-succeeded run. Persisted scope CHECK includes it. workshop_sync_health selects both reserved scopes and its cached last_success_at has mixed provenance. Legacy finish can mark a run succeeded when passed a null cursor; it does not take a run lease token/fence. Task refresh uses its own authorized task lookup and shared per-order reconcile path.
- (decision) Retirement belongs to stable CAP-1, not a new capability: distinguish active execution validation from historical scope representation. Reject all_reserved starts/resumes and forged scope/run combinations in actions, cursor-to-execution paths, workers and public/private database execution entry points before provider work; retain the historical value and rows/results for reads. Shared low-level result/finalization paths must not revive or mutate retired runs.
- (decision) Retire unfinished all_reserved runs idempotently with an explicit non-success retirement reason/time: in_progress becomes failed using the existing state model; already failed rows remain failed and non-resumable. Preserve scope, IDs, counters, results, prior errors/times and old cursor as inert evidence; add retirement evidence without erasing prior failure details and fill a missing completion time where needed. Existing succeeded rows keep their outcome. Never call the ordinary successful finish path just to clear a cursor.
- (constraint) Cutover must stop/drain pre-change all_reserved workers or prove equivalent rejection at apply/record/finish boundaries before retirement; run lease invalidation alone does not fence shared per-order apply. Reject delayed writes and release/invalidate only leases identified as belonging to retired runs with matching ownership/fence; do not disturb seven-day, Dashboard, task or webhook work. Do not roll back source/task changes already committed by legacy runs.
- (constraint) Exclude all_reserved from active recovery, pending/busy state and current-scope freshness; preserve its historical evidence. Workshop freshness must come from successful next_7_days runs, not the mixed legacy health timestamp. Active legacy seven-day cursors retain compatible recovery; all_reserved cursors receive an explicit retirement error with no scope conversion or automatic substitute run.
- (constraint) Preserve individual task refresh with existing authorization/environment checks, task-to-order lookup, per-order lease and guarded reconciliation. It remains an independent single-order operation and is not constrained to either page date range.
- (event) Activation/config re-resolved for retirement update; no hooks configured and project-context.md remains absent. Rich user direction supersedes the earlier compatibility assumption; current source inspection supplies the unfinished-run treatment. No implementation, migration or database inspection authorized or performed.
- (event) Coherence pass passed for retirement update: stable CAP-1/CAP-2/CAP-3 retained; active execution allows only next_7_days/selected_period page scopes while historical representation retains all_reserved. Existing failed state plus explicit retirement evidence closes unfinished runs without success/reinterpretation. Individual task refresh and remaining optimization requirements are consistent across kernel and three companions; the superseded executable-compatibility assumption is removed.
- (event) Preservation pass passed: all user retirement requirements mapped to SPEC constraints/CAP-1 and sync-contract retirement/task sections, current entry-point findings retained in brownfield, and direct-call/history/cursor/lease/stale-worker/freshness/idempotence/task checks in verification. Earlier Dashboard reuse, CAP-2 accounting, conditional CAP-3, date/retry/security and rollout constraints preserved. Wrapper-only content: repeated request wording and trailing typo omitted.
- (event) Retirement spec update complete: re-derived four contract documents after appending decisions; structure/reference/retirement coverage checks and git diff --check passed. Only the same spec folder and its append-only log changed; no application implementation, migrations, database/provider calls, runtime tests or commit. Exact retirement metadata storage and worker cutover mechanism remain implementation design subject to the recorded acceptance criteria.
- (direction) User accepts the simple transition policy after explaining there are about 400 orders and syncs are short: if an old unfinished run is encountered after the update, close it, preserve history and let staff start a fresh sync. Do not reconstruct old progress or make this rare transition a separate implementation blocker.
- (decision) Supersede compatible legacy seven-day cursor recovery with close-and-restart for unfinished pre-update page-level runs. Keep original scope, IDs, results, counters, errors and inert cursor evidence; record non-success closure once. No transfer of old page offsets/results into a new run, no inferred original date window, no auto-substitution for an all_reserved request. Completed historical runs remain historical.
- (decision) For runs created by the new implementation, keep the saved dates during processing. A new Workshop click targets today through six days later in Madrid and may resume only a compatible unfinished run for that same interval; otherwise it starts fresh. Dashboard keeps exact selected-period recovery. Completion belongs to saved dates; older or unknown bounds cannot assert current-window coverage. This is ordinary date handling without extra UI or background rollover behavior.
- (constraint) Use existing lease/busy behavior before closing an interrupted old run; do not stop a healthy active worker or clear unrelated locks. Closed runs cannot be resumed or overwritten by delayed result/finish calls. Supersede the bespoke stop/drain/cutover-proof requirement with this bounded close-and-restart behavior; no reconstruction migration, cancellation subsystem or worker-draining project is required.
- (event) bmad-spec update activation re-resolved with no custom hooks; same slug and stable capability IDs retained. Supersession is limited to legacy recovery/cutover complexity and date attribution; all_reserved execution rejection/history retention, individual task refresh, shared counting and conditional batching remain required.
- (event) Coherence pass passed: pre-update unfinished runs close with history preserved; new-format exact-interval recovery remains; midnight handling keeps saved dates and new clicks use current dates. Removed legacy cursor translation and bespoke draining requirements. CAP-1/CAP-2/CAP-3 IDs and all_reserved execution retirement remain consistent across the kernel and three companions.
- (event) Preservation pass passed: accepted simple close-and-start-fresh policy and user volume context retained; prior history retention, action/database rejection, task refresh, Dashboard reuse, counting correctness, conditional bulk detail gate and local-only rollout boundaries preserved. Earlier compatible-legacy-recovery and cutover-proof decisions are explicitly superseded. Wrapper-only content: review scores and repeated discussion omitted.
- (event) Update complete: re-derived SPEC.md, sync-contract.md, brownfield.md and verification.md; document checks and git diff --check passed. No code, migration, provider/database action or runtime test performed; changes remain uncommitted.
